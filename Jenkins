pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "AWS_CONFIG_FILE=/home/ubuntu/.aws/config aws cloudformation create-stack --stack-name ecinstance10 --template-body file://ec2.yaml --tags Key=Name,Value=114poc01 --region 'us-east-1'"
              }
             }
            }
            }
